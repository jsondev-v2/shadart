import 'package:flutter/material.dart';
import '../blocks/blocks_showcase.dart';

class BlocksScreen extends StatelessWidget {
  const BlocksScreen({super.key});
  @override
  Widget build(BuildContext context) => const BlocksShowcase();
}
